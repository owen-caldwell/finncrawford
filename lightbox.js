// Elements: Lightbox wrapper, lightbox image, lightbox buttons, images in gallery
// If an image is selected, display the lightbox wrapper. Get selected image src and fill
// lightbox image attribute with it. If the lightbox wrapper is clicked, hide it.
// If arrow
const imgz = document.querySelectorAll(".gallery img");
let lbimage = document.querySelector("#lightboximage");
let lb = document.querySelector("#lightbox-wrapper");

imgz.forEach(img => {
    img.addEventListener("click", e => {
        
    });
});